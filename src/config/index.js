const config = {
  origin: 'http://localhost:8080/',
  saveUrl: window._page_httpUrl.saveUrl,
  dev: process.env.NODE_ENV === 'development',
  baseImgPath: '/assets/',
  totalTime: 60
}
export default config
